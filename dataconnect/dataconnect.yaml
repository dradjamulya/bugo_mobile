specVersion: "v1"
serviceId: "bugomobile"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bugomobile-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
